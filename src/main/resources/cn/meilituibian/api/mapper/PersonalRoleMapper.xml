<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.meilituibian.api.mapper.PersonalRoleMapper">
    <select id="getPersonalRoleByJobTitle" resultType="cn.meilituibian.api.domain.PersonalRole">
        select id,role_name AS roleName,role_code AS roleName, menus, job_title AS jobTitle from personal_role where job_title = #{jobTitle}
    </select>

</mapper>