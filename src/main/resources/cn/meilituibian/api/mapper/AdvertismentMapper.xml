<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.meilituibian.api.mapper.AdvertismentMapper">
    <select id="getAdvList" resultType="Advertisment">
        select * from advertisment where code=#{code}
    </select>
</mapper>
